% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/code_spec_api.R
\name{code_spec}
\alias{code_spec}
\title{Create a user-defined condition code specification}
\usage{
code_spec(
  condition,
  label,
  version = NULL,
  defs = list(condition = NULL, outcome = NULL),
  codes = list(),
  exclusions = NULL
)
}
\arguments{
\item{condition}{Short identifier string, e.g. \code{"my_cond"}.}

\item{label}{Human-readable label, e.g. \code{"My Condition"}.}

\item{version}{Optional version label string, e.g. \code{"v1"}. Stored as
metadata only; use distinct object names (e.g., \code{my_spec_v1}) to
distinguish versions.}

\item{defs}{Named list with elements \code{condition} and/or \code{outcome}, each a
character string describing the algorithm. \code{NULL} elements are allowed.}

\item{codes}{Named list of key-level lists keyed by \verb{\{code_type\}_\{encounter\}}
(e.g., \code{"dx_icd9"}). Each element may be:
\itemize{
\item A character vector of codes — both \code{condition} and \code{outcome}
flags are set to \code{TRUE} for all codes.
\item A list with elements \code{codes}, \code{condition} (logical
vector), \code{outcome} (logical vector), and optionally \code{exclusions}.
}}

\item{exclusions}{Optional named list of exclusion note strings, one per
code-set key.}
}
\value{
A \link{CodeSpec} R6 object.
}
\description{
Constructs a \link{CodeSpec} R6 object from user-supplied codes and definitions.
Codes should be provided in \strong{exact short format} (no decimal periods,
e.g., \code{"4010"} not \code{"401.0"}).
}
\examples{
my_spec <- code_spec(
  condition = "test",
  label     = "Test Condition",
  version   = "v1",
  codes     = list(dx_icd9 = c("4010", "4011", "4019"))
)
my_spec
}
