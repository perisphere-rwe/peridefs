% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drugs.R
\name{get_diabetes_generics}
\alias{get_diabetes_generics}
\alias{get_diabetes_meds_labels}
\title{Retrieve generic drug names for diabetes medications}
\usage{
get_diabetes_generics(component = NULL, priority = 1L, condition = NULL)

get_diabetes_meds_labels(component = NULL)
}
\arguments{
\item{component}{Optional named component (e.g. \code{"acei_v1"}) for composite
specs. \code{NULL} (default) or \code{"all"} returns every component's generics.
Print the composite spec to see all available component names.}

\item{priority}{Integer vector subsetting confidence tiers to include
(\code{1} = core, \code{2} = probable, \code{3} = cautious). Default \code{1}.}

\item{condition}{Optional character vector subsetting to specific
condition(s). \code{NULL} (default) uses the composite's own condition
(e.g. \code{get_obesity_generics()} defaults to \code{"obesity"}), so a leaf
component shared across composites (e.g. a GLP-1 spec used by both
the obesity and diabetes composites) only contributes its rows for
\emph{this} composite's condition. Pass a value explicitly to widen or
otherwise override the default.}
}
\value{
\verb{get_*_generics()}: a tibble with columns \code{generic}, \code{brand},
\code{priority}, \code{condition}, \code{class}, and \code{version}. \verb{get_*_meds_labels()}:
a tibble with columns \code{name} and \code{label}.
}
\description{
\code{spec_diabetes} is a \link{CompositeDrugSpec} containing all versioned
antidiabetic leaf specs: \code{biguanide_v1}, \code{sulfonylurea_v1},
\code{meglitinide_v1}, \code{tzd_v1}, \code{alpha_glucosidase_v1}, \code{dpp4_v1},
\code{sglt2_v1}, \code{glp1_v1}, \code{insulin_v1}, \code{amylin_v1}.
}
\seealso{
\code{spec_diabetes}
}
