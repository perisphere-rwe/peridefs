% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expand_pcs.R
\name{expand_pcs}
\alias{expand_pcs}
\title{Expand ICD-10-PCS prefix patterns to all matching valid codes}
\usage{
expand_pcs(patterns)
}
\arguments{
\item{patterns}{Character vector of ICD-10-PCS pattern strings. Examples:
\itemize{
\item \code{"0210xxx"} — 4-character prefix, returns all 74 CABG variants
\item \code{"03CH0ZZ"} — exact code, returned as-is if valid
\item \code{c("0210xxx", "0211xxx")} — vectorised; results are unioned
}}
}
\value{
Character vector of unique valid ICD-10-PCS codes (7 characters,
no periods).
}
\description{
Resolves ICD-10-PCS pattern strings of the form used in the Perisphere
definitions document (e.g., \code{"0210xxx"}) into the full set of valid
7-character ICD-10-PCS codes from the FY2026 CMS order file.

Patterns are matched by \strong{prefix}: trailing lowercase \code{x} characters
are stripped to form the prefix, and all valid codes that begin with that
prefix are returned. Codes with no trailing \code{x} are treated as exact codes
and returned only if they exist in the reference table.
}
\examples{
# All CABG codes (0210 prefix)
expand_pcs("0210xxx")

# Exact code lookup
expand_pcs("02100Z9")

# Multiple patterns — results are combined and deduplicated
expand_pcs(c("0210xxx", "0211xxx"))
}
